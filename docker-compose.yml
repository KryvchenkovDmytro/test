version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: yii2_api
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./:/var/www
    networks:
      - yii2_network

networks:
  yii2_network:
    driver: bridge
